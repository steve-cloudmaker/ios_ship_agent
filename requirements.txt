# iOS Ship Agent — Python Dependencies
# Install with: pip install -r requirements.txt

# Core AI
anthropic>=0.25.0
openai>=1.25.0

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# HTTP (scraping)
requests>=2.31.0

# Image generation / processing
Pillow>=10.3.0

# Terminal UI
rich>=13.7.0

# App Store Connect JWT (only needed when AUTO_SUBMIT=true)
PyJWT>=2.8.0
cryptography>=42.0.0

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
